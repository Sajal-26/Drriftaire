# Frontend API Configuration
# The API URL should point to your backend (development or production)
# For Vercel: https://your-vercel-domain.vercel.app/api
VITE_API_URL=http://localhost:3000/api

# Health check endpoint (same as VITE_API_URL but without /api)
# Used for verifying backend connectivity
